extends:
  - recommended

rules:
  # Los servers deben ser URLs reales; localhost (desarrollo local) se exceptúa en .redocly.lint-ignore.yaml.
  no-server-example.com: error
  # La licencia es propietaria (uso exclusivo del reto): no existe una URL pública que referenciar.
  info-license-strict: off
